<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.javalec.ex.Dao.EDao">

	<select id="event_list" resultType="com.javalec.ex.Dto.EDto.EventDto">
		select * from event order by e_code desc
	</select>
	<select id="getListCount" resultType="Integer">
		<choose>
			<when test="">SELECT count(*) as count FROM event</when>
			<when test="">SELECT count(*) as count FROM mvc_board WHERE bTitle LIKE ? OR bContent LIKE ?</when>
		</choose>
	</select>




</mapper>